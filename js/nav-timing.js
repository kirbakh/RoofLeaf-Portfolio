/** Затримка перед переходом на іншу сторінку (мс) */
export const PAGE_TRANSITION_MS = 320;
export const MOBILE_PAGE_TRANSITION_MS = 280;

/** Intro-loader: bloom / complete / finishDelay (мс) */
export const INTRO_LOADER_DESKTOP = { bloom: 2800, complete: 4600, finishDelay: 950 };
export const INTRO_LOADER_MOBILE = { bloom: 900, complete: 1850, finishDelay: 580 };

export const ROUTE_LOADER_MIN_MS = { desktop: 520, mobile: 340 };
