/** Затримка перед переходом на іншу сторінку (мс) */
export const PAGE_TRANSITION_MS = 320;
export const MOBILE_PAGE_TRANSITION_MS = 280;

/** Intro-loader: bloom / complete / finishDelay (мс) */
export const INTRO_LOADER_DESKTOP = { bloom: 2400, complete: 3800, finishDelay: 800 };
export const INTRO_LOADER_MOBILE = { bloom: 650, complete: 1150, finishDelay: 420 };

export const ROUTE_LOADER_MIN_MS = { desktop: 420, mobile: 260 };
